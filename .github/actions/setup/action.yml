name: 'Setup'
description: 'Checks out code and sets up the JDK'
runs:
  using: "composite"
  steps:

    - name: Set up JDK 17
      uses: actions/setup-java@v2
      with:
        java-version: 17
        distribution: 'zulu'
